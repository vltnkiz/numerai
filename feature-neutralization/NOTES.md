# Notes

- Mission: user is building a Numerai model; goal is practical (reduce feature exposure risk, implement neutralization), not academic.
- Background: new to both ML and statistics. Don't assume correlation/regression/pseudo-inverse are known — teach them inline, minimally, as needed.
- Two trusted sources supplied by the user up front (see RESOURCES.md) — ground all claims in these rather than parametric knowledge.
- Course sequencing: Lesson 1 = feature exposure (the diagnostic/problem). Lesson 2 = correlation & Spearman (the measurement tool). Lesson 3 = feature neutralization (the fix, regression/projection math + proportion knob). Lesson 4 = visualizing fitted/residual (draggable scatter). Lesson 5 = implementing neutralization in code, grounded in the real numerai-tools `neutralize` function (scalar β → vector of β's via np.linalg.lstsq). Lesson 6 = matrix multiplication & inverse (prerequisite gap surfaced by Lesson 5's Xβ / pinv(X), taught via 3Blue1Brown + the exact numbers from Lesson 5's worked example). Next: era-wise looping and the official example-scripts notebook (see RESOURCES.md gaps).
- Pattern to reuse: when a lesson's code/math leans on an unexplained prerequisite (e.g. matrix multiplication), it's fine to branch off a dedicated prerequisite lesson rather than cramming it inline — but ground it in the exact numbers from the lesson that motivated it, not a fresh abstract example, so it reads as reinforcement rather than a detour.
