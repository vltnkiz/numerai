# Baseline background: new to both ML and stats

The user disclosed they are newer to both machine learning and statistics — not just to feature neutralization
specifically. Concepts like correlation, regression, and orthogonal projection/pseudo-inverse cannot be assumed;
they need to be introduced alongside the finance-specific content rather than referenced as known prerequisites.
This sets the floor for how much scaffolding every future lesson in this course needs, particularly Lesson 2
(feature neutralization), which relies on least-squares regression and the Moore-Penrose pseudoinverse.
