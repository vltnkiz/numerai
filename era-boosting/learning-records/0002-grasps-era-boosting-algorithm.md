# Grasps the era boosting algorithm mechanics

User confirmed lesson 2 ("The Era Boosting Algorithm") was clear — no corrections needed. They now understand the build → score per-era corr → keep worst `proportion` → retrain loop, including that era membership in the "worst half" is re-evaluated every round (not fixed). This closes out the knowledge phase of the mission ([[MISSION.md]] success criteria: explain the algorithm end to end) — the next lesson can move to skills practice (hands-on implementation) without re-explaining the loop itself.
